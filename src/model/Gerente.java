package model;

public class Gerente extends Funcionario {
    // Esta classe herda tudo de Funcionário.
    // Usamos o tipo da classe para diferenciar as permissões.
    public Gerente() {
        super();
    }
}